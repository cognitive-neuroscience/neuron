package controllers

import (
	"github.com/gofiber/fiber"
)

// SaveTurker saves the given worker id and experiment code that is being completed
func SaveTurker(c *fiber.Ctx) {
	// handle save turker, can be the same worker or experiment code but cannot be the same combination
}
